{
	"name": "crypto_box_open_detached",
	"type": "function",
	"inputs": [
		{
			"name": "ciphertext",
			"type": "unsized_buffer"
		},
		{
			"name": "mac",
			"type": "buffer",
			"size": "libsodium_raw._crypto_box_macbytes()"
		},
		{
			"name": "nonce",
			"type": "buffer",
			"size": "libsodium_raw._crypto_box_noncebytes()"
		},
		{
			"name": "publicKey",
			"type": "buffer",
			"size": "libsodium_raw._crypto_box_publickeybytes()"
		},
		{
			"name": "secretKey",
			"type": "buffer",
			"size": "libsodium_raw._crypto_box_secretkeybytes()"
		},
		{
			"name": "encoding",
			"type": "encoding"
		}
	],
	"outputs" : [
		{
			"name": "plaintext",
			"type": "buffer",
			"size": "ciphertext_size"
		}
	],
	"target": "var decryptSuccess = libsodium_raw._crypto_box_open_detached(plaintext_byte, ciphertext_byte, mac_byte, ciphertext_size, 0, nonce_byte, publicKey_byte, secretKey_byte)",
	"return": "return (decryptSuccess == 0 ? decode_utf8(plaintext.extractBytes()) : undefined)"
}
